zen-extensions
.config/zen/*/chrome/zen-themes.css

# noctalia-shell replaces these; sources kept in repo but no longer deployed
.config/swayidle
.config/swaylock
.local/share/applications/swaylock.desktop

**/__pycache__
.claude/skills/synced

# local Pi package deps: reinstall with npm install --omit=dev --omit=peer
.pi/agent/local-packages/*/node_modules
