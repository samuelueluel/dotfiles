# Rendered by chezmoi from the exported values in ~/.zshenv.local.
# Keep this file mode 0600; do not commit rendered values.
{{ $us := env "OPENROUTER_API_KEY_US" }}
{{ $ds := env "OPENROUTER_API_KEY_DS" }}
{{ if not $us }}{{ fail "OPENROUTER_API_KEY_US is not available while applying chezmoi" }}{{ end }}
{{ if not $ds }}{{ fail "OPENROUTER_API_KEY_DS is not available while applying chezmoi" }}{{ end }}
OPENROUTER_API_KEY_US={{ $us }}
OPENROUTER_API_KEY_DS={{ $ds }}
