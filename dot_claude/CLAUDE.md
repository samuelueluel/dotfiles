@~/.pi/agent/APPEND_SYSTEM.md

## Claude Code Notes

- This is Claude Code, not Pi. Pi-specific launcher rules (`/mcp enable`, sandboxing) do not apply; `claude` runs unsandboxed on the host without Stata. Use `claudebeta` for Stata work.
- `sudo` is unavailable: ask Samuel for one-liners, or write multi-step commands to `~/sudo_temp.sh` for `sudo bash ~/sudo_temp.sh`.
